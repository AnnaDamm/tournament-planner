services:
  host:
    build:
      context: .
      target: production
    ports:
      - '${TOURNY_PORT:-8080}:8080'
    environment:
      TOURNY_PORT: 8080
      TOURNY_VIEWER_URL: ${TOURNY_VIEWER_URL:?Set it to the laptop LAN URL, for example http://192.168.178.42:8080/}
